export enum viewTaskActions {
    OPEN_DIALOG = 'OPEN_VIEW_TASK',
    CLOSE_DIALOG = 'CLOSE_VIEW_TASK',
}

export enum key {
    ESCAPE = 'Escape',
    DELETE = 'Delete'
}

export enum eventListener {
    KEY_UP = 'keyup',
}
